extends ../layout/index

mixin knob(trknum, label, myclass, low, high)
    div.row
        div(class=myclass + " track-knob-eq")
            label(class="col-md-12")=label
            span=low
            input(type='text')
            span=high